# 2026-07-19 工作记录

## 项目：AI 试衣魔镜（虚拟试衣网站）
- 技术栈：React 19 + TypeScript + Vite 7 + Tailwind v3.4 + shadcn/ui（modern-web-app skill 脚手架，输出在 `./app`）
- 需求：用户粘贴购物平台链接 → 识别服装 → 上传自拍 →（可选）填身体数据 → 生成试衣效果图
- 已完成首版（MVP），可完整跑通：
  - `src/lib/parseLink.ts`：平台识别 + 服装类目/类型/颜色识别（前端确定性 mock，含生产接入说明：后端代理/开放API/无头浏览器）
  - `src/lib/garments.ts`：按类型+颜色生成服装 SVG（透明背景，离线、无 CORS 问题）
  - `src/lib/tryon.ts`：Canvas 合成试衣「智能预览」，依据身体数据估算躯干区域贴合；含逼真度评分与生产 AI 模型接入说明（IDM-VTON/OOTDiffusion/Replicate/图生图）
  - `src/lib/sampleData.ts`：示例自拍(SVG人物)+示例链接，支持「一键体验」
  - 组件：StepIndicator / LinkInput / ProductCard / SelfieUpload(拖拽+拍照+示例) / BodyMeasurements / TryOnResult(前后对比+下载)
  - `src/App.tsx`：四步流程串联 + Toaster + 隐私说明
- 构建：`npm run build` 通过；预览 `npm run preview -- --port 4173`
- 部署：CloudStudio 沙箱，分享链接 https://d3c6a4077f0f4baa84d9d50de2d8a20c.app.codebuddy.work
- 关键点：SVG 无固有尺寸，tryon 用固定 viewBox 比例 200/260 计算宽高比（勿用 gImg.width）

## 第二轮：接入真实后端 + AI 试衣 + HEIC（2026-07-19 下午）
- 新增后端 `./server`（Node + Express，端口 8787）：
  - `GET /api/parse?url=`：抓取页面，提取 OG/Twitter/JSON-LD（标题/主图/价格/店铺），服装关键词识别（已用 apple.com 实测通过）
  - `POST /api/tryon`：Replicate IDM-VTON/OOTDiffusion 可插拔（env: REPLICATE_API_TOKEN / MODEL_VERSION / MODEL），无 token 返回 503
  - 依赖：express/cors/cheerio/dotenv；README + .env.example 已写
- 前端智能路由（无后端/失败自动回退，静态站点仍可跑）：
  - 新增 `src/lib/api.ts`（API_BASE=VITE_API_BASE||/api）
  - `parseLink.ts` 增 `parseProduct()`：优先后端，失败回退 mock；ProductCard 显示真实主图+「真实解析/示例数据」徽标
  - `tryon.ts` 增 `generateTryOn()`：优先调 /api/tryon（服装 SVG 栅格化 PNG 作输入），失败回退 Canvas 预览；结果区分「AI 试衣/本地预览」
  - `vite.config.ts` 加 dev proxy `/api`→8787
- HEIC：`app` 装 heic2any@0.0.4；`SelfieUpload` 检测 .heic/.heif → 浏览器端 wasm 转 JPEG（动态 import，仅用时加载，分块 1.35MB）
- 验证：`npm run build` 通过；dev 代理→后端→真实解析实测通过；预览 4173 正常
- 部署：已 redeploy 到同一 CloudStudio 链接（静态站无后端→回退 mock/canvas；本地 `npm run dev`+`node server` 走真实后端）

## 第三轮：AI 试衣改 OpenAI 兼容图像生成（2026-07-19 晚）
- 用户要求：不要用 Replicate token；改用 OpenAI 兼容风格的图像生成能力，把衣服+自拍拼成一张图；只需配 base_url / api_key / model
- 重写 `server/tryon.js`：去掉 Replicate，改为调 OpenAI 兼容图像接口
  - 默认端点 `/images/edits`（`IMAGE_BASE_URL + IMAGE_ENDPOINT`，务必避免双重 /v1）
  - edits 模式：自拍照作 multipart `image` 输入 + 服装描述 prompt（含颜色/类型/覆盖部位/身体数据），由模型把服装「穿」到身上
  - 支持 generations 模式（IMAGE_INPUT_IMAGE=false，纯文生图）
  - 兼容返回 b64_json 或 url（url 会再 fetch 转 dataURL 防跨域/失效）
  - env：IMAGE_BASE_URL(默认 https://api.openai.com/v1) / IMAGE_API_KEY / IMAGE_MODEL(默认 gpt-image-1) / IMAGE_ENDPOINT / IMAGE_SIZE(1024x1024) / IMAGE_QUALITY(auto) / IMAGE_INPUT_IMAGE(true)
  - 未配 IMAGE_API_KEY/IMAGE_MODEL → 503 NO_TOKEN，前端回退 Canvas
- `server/index.js`：health 改报 image 配置；/api/tryon 入参改 `{ selfie, garment{type,color,region}, measurements }`
- 前端 `src/lib/api.ts`：TryOnApiPayload 改为结构化 garment 对象 + measurements；`src/lib/tryon.ts` generateTryOn 不再栅格化服装，直接发结构化数据；`src/App.tsx` 更新隐私说明（AI 模式会经后端转发给模型）与按钮提示文案
- 验证：`npm run build` 通过；后端假 key 实测返回 401（确认路径 `.../v1/images/edits` 与 multipart 正确）；无 key 返回 503 回退
- 已 redeploy 到同一 CloudStudio 链接
- 接法：用户只需 `cp .env.example .env` 后填 IMAGE_BASE_URL / IMAGE_API_KEY / IMAGE_MODEL 即可

## 第四轮：接入 Agnes Image 2.0 Flash（2026-07-19）
- 用户给了文档 https://agnes-ai.com/zh-Hans/docs/agnes-image-20-flash
- Agnes 接口：POST {base}/images/generations（JSON 体），Bearer 鉴权
  - 模型名 `agnes-image-2.0-flash`，基地址 `https://apihub.agnes-ai.com/v1`
  - 文生图：仅 model/prompt/size；图生图：自拍照放 `extra_body.image`（数组，支持 Data URI）
  - 输出格式 `extra_body.response_format: "b64_json" | "url"`（勿放顶层）
  - 响应 data[0].b64_json（原始 base64，需加 data:image/png;base64, 前缀）或 data[0].url
- 改写 `server/tryon.js`：去掉 multipart edits，改为 Agnes JSON 风格（extra_body.image + response_format=b64_json）
  - 移除 IMAGE_QUALITY（Agnes 不支持）；保留 IMAGE_INPUT_IMAGE 控制是否图生图
  - 提示词沿用「保留人脸/身形/姿势，仅加服装」结构
- `.env.example` 默认值改为 Agnes（base/model/endpoint）；README 第二节同步更新
- 验证：`npm run build`（前端未改，仅服务端）；假 key 实测 Agnes 返回 401 无效令牌 → 证明 URL/鉴权/JSON 体正确；无 key 返回 503 回退
- 用户只需 `cp .env.example .env` 填 IMAGE_API_KEY 即可用 Agnes 跑真实试衣

## 第五轮：修复淘宝链接无法识别（2026-07-19）
- 根因：淘宝等强反爬，后端无头 fetch 被拦截，拿不到商品标题（title 回退成 URL 本身）→ isClothing:false、garments:[] → 前端判「非服装/无法识别」
- 修复：
  - `server/parse.js`：新增反爬检测 `isAntiCrawl`（标题为空/等于URL/含验证码·登录·淘你喜欢等，或淘宝系无 og:image）+ `extractItemId` + `buildFallback`；抓不到详情时，对购物平台返回 `isClothing:true + incomplete:true + 提取到的 itemId`，不再死判非服装；普通网页仍 isClothing:false
  - `src/types/index.ts`：ParsedProduct 增 `incomplete?` / `itemId?`
  - `src/components/ProductCard.tsx`：garments 为空时渲染「手动选择服装」面板（类型网格 + 色板，确认后 onAddManual 注入 garment）；incomplete 时琥珀色提示反爬拦截
  - `src/App.tsx`：新增 handleAddManual，把手动 garment push 进 product.garments 并选中，推进流程
  - `src/lib/parseLink.ts`：mock 路径对购物平台链接强制 isClothing:true（离线兜底也能识别）
  - `src/components/LinkInput.tsx`：从分享文案/淘口令中提取 http 链接；非 http 且像淘口令时友好提示「请粘贴以 http 开头的完整商品链接」
- 验证：后端实测 淘宝/天猫 item 链接 → isClothing:true+incomplete+itemId；普通网页 → isClothing:false；`npm run build` 通过；已 redeploy 同一 CloudStudio 链接
- 结论：淘宝链接现已可识别并试衣（有后端→手动选服装；无后端静态站→mock 随机服装）。真实商品标题/主图仍受反爬限制，需登录态 Cookie 或开放平台 API

## 第六轮：接入淘宝登录态 Cookie（2026-07-19）
- 用户选择用「淘宝登录态 Cookie」而非联盟 API 来获取淘宝/天猫真实商品数据（个人无企业资质时最实际的路径）
- `server/parse.js`：
  - 新增 `TAOBAO_COOKIE` 环境变量 + `requestHeaders(url)`：淘宝/天猫链接自动带 `Cookie` 头（含 Referer），其它平台不带（已用真实函数单测验证：淘宝/天猫附加、京东不附加）
  - 增强提取：`extractEmbedded()` 兜底解析页面内嵌 `<script>` JSON 的 title/price/image；`stripTitleSuffix()` 去掉「 - 淘宝网/天猫」等后缀
  - 调整 `isAntiCrawl`：不再因「淘宝页无 og:image」就判为拦截（带 cookie 时可能已有真实标题），改由调用方按 hasImage 标 incomplete
  - 解析结果增 `incomplete: !hasImage` 与 `cookieUsed`（淘宝/天猫且配了 cookie 为 true）；敏感 cookie 绝不打印（已验证日志 0 泄露）
- `server/.env.example`：新增 `TAOBAO_COOKIE` 及获取方式说明（F12→Network→复制商品页请求的 Cookie 整段；会过期需重填；勿泄露/勿提交 git）
- `server/index.js`：启动日志报告 cookie 是否配置
- `server/README.md`：第一节补充 Cookie 用法
- 前端：`src/types/index.ts` 增 `cookieUsed?`；`ProductCard.tsx` 增「登录态识别」橙色徽标 + 调整 incomplete 提示文案（更准确）
- 验证：`node --check` 通过；requestHeaders 单测通过；端到端：带 dummy cookie 解析淘宝 → 正常返回 cookieUsed:true（dummy 会被拦截→incomplete 兜底）；无 cookie 时淘宝/京东/普通网页均正确回退；`npm run build` 通过；已 redeploy 同一 CloudStudio 链接
- 注意：Cookie 需用户自己填真实登录态才出真实标题/主图；京东反爬同样重，本轮只做淘宝/天猫 cookie，京东仍走兜底

## 待办 / 后续方向
- 淘宝 Cookie 需用户自行填入真实登录态（会过期，失效后重填）才出真实数据
- 京东/拼多多反爬仍重，若要真实数据需各自登录态 Cookie 或联盟 API（server/parse.js 可扩展）
- 若更换非 Agnes 的 OpenAI 兼容服务且用 multipart /images/edits，需改 callImageApi（代码注释已标注改动点）
- 可加：多服装对比、分享、历史记录、深色模式切换；服装自动抠图（从主图抠出 garment 提升贴合度）

## 第七轮：修复淘宝"图片"类链接被误判非服装（2026-07-19）
- 用户反馈：粘贴一条淘宝链接（标题是"图片/自定义"类，无 T恤/衬衫等服装词），提示「该链接商品不是服装类，暂不支持试衣」
- 根因：用户已在本地后端配了 `TAOBAO_COOKIE`，抓到了真实标题，但 `parse.js` 正常路径里 `isClothing = isClothingTitle(title)` 因标题无服装关键词返回 false → 直接判非服装
- 修复（`server/parse.js` 正常解析路径）：
  - 改为按平台分两类：① 未知平台（example.com 等）才按关键词判定，可返回 isClothing:false；② 购物平台链接（淘宝/天猫/京东/拼多多等）一律 `isClothing:true`——标题命中服装词→自动识别 garment，未命中→标 `incomplete:true`、garments:[]，由前端弹「手动选服装」面板继续，绝不再硬拒
- 验证：模拟 fetch 三类页面（淘宝无服装词/淘宝含T恤词/普通网页）→ 分别得到 isClothing:true+incomplete / isClothing:true+tshirt / isClothing:false，全部符合预期
- 影响：用户重启本地后端后即生效；无需改前端（手动选服装面板已存在）。README 第一节补充了判定规则

## 第八轮：淘宝短链识别 + 换脸式试衣（2026-07-19）
- 需求①：识别手机分享的淘宝短链 `https://e.tb.cn/h/xxx?tk=...`
  - 根因：短链落地页是 JS 跳转（HTTP 200），真实商品链接 `item.taobao.com/item.htm?id=...` 嵌在 HTML 里，旧逻辑 `detectPlatform` 不认 `tb.cn` 且 fetch 跟不到 JS 跳转
  - `server/parse.js`：
    - `PLATFORM_RULES` 淘宝加 `|tb\.cn`（覆盖 e.tb.cn/m.tb.cn）
    - 新增 `extractRedirectTarget(html)`：从落地页 HTML 正则提取 `item.taobao.com/item.htm?id=...`（兼容 `&amp;` 与 href/location 写法）
    - `parseProductPage` 改为两跳：先抓落地页 → 提取真实商品链接 → 用带 Cookie 的请求二次抓取商品页；`finalUrl` 用于 detectPlatform/extractItemId/buildFallback
  - 前端 `parseLink.ts` 的 PLATFORM_RULES 同步加 `tb.cn`（mock 兜底也能识别短链）
  - 验证：模拟 fetch 三跳通过；真实短链经 `/api/parse` 实测解析到 item.taobao.com?id=802805668523 并提取到真实主图+价格+标题，isClothing:true
- 需求②：合成试衣时保留商品图里模特的姿势/服装，仅把用户自拍的脸合成上去（换脸式）
  - `server/tryon.js`：
    - `runTryOn` 新增 `productImage`（URL 或 dataURL）；有则 `resolveToDataUrl` 服务端拉取转 base64（跨域 URL 在服务端抓取，规避前端 CORS）
    - `buildPrompt` 加 mode：productImage 存在→「换脸式」prompt（保留第一张图构图/姿势/服装/背景，仅替换人脸）；否则回退「描述式」prompt（在自拍上加服装）
    - `callImageApi` 多图输入：`images=[productImage, selfie]`，顺序即「商品图为底、自拍提供脸」；仍兼容 IMAGE_INPUT_IMAGE=false（纯文生图）
  - `server/index.js` /api/tryon 入参加 `productImage`；前端 `api.ts` TryOnApiPayload 加 `productImage?`；`tryon.ts` generateTryOn 透传 `productImageUrl`；`App.tsx` 调 generateTryOn 时传 `product?.imageUrl`，并更新隐私说明（商品主图也会随自拍发给模型）
  - 验证：模拟 fetch 验证 ① 换脸模式 images=[商品jpg, 自拍png] 且 prompt 含保留姿势/仅换脸 ② 无商品图回退 images=[自拍]；`npm run build` 通过；已 redeploy 同一 CloudStudio 链接
- 注意：换脸效果取决于 Agnes 对多图输入的支持（extra_body.image 数组）；若模型只用首图，可在 callImageApi 调整图序或改用 /images/edits（注释已留改动点）
- 注意：商品主图为跨域 URL 时由后端 resolveToDataUrl 抓取；若抓取失败则自动回退描述式模式（不影响流程）

## 第九轮：回滚「第八轮之后的真试衣优化」（2026-07-19 下午）
- 背景：上一轮（非本记录第8轮之外的一次优化）我把试衣改成了「真试衣（true try-on）」——以自拍为基图、把若干服装照片穿到身上，并新增多服装上传 GarmentUpload、true/tryon 模式、lib/image.ts 共享工具等。
- 用户反馈：理解错了需求，要回滚这次修改。用户真正要的是第八轮定的「换脸式」效果（保留商品图姿势/服装，仅合成自拍的脸），不要多服装上传/真试衣那套。
- 回滚方式：`git reset --hard a8f8a1e`（提交 66ad72e「fix: add image」即含那次优化，a8f8a1e「init」即换脸式版本，且含之前所有修复：短链两跳、淘宝图片类误判、Cookie 等）
  - 还原后：tryon.js 为换脸式（runTryOn 仅 selfie/garment/measurements/productImage，无 garmentImages/mode）；GarmentUpload.tsx 与 lib/image.ts 已删除；App.tsx 走 productImageUrl: product?.imageUrl 换脸流程
- 验证：`git status` 干净；`npm run build` 通过；已 redeploy 到同一 CloudStudio 链接
- 教训：用户要的"试衣润色"是指"换脸效果更自然/清晰"，不是改成"真试衣"。下次动核心交互（换模式/加上传组件）前应先与用户确认，避免误改已定方案。

## 第十轮：商品信息"只拿到图、拿不到标题/价格" → 视觉识别自动补全（2026-07-19）
- 用户现象：粘贴淘宝链接后只拿到商品主图，标题/价格/服装类型缺失，被迫手动选。
- 根因（server/parse.js）：淘宝真实标题/价格在 JS 内嵌数据里，`extractMeta/extractEmbedded` 键名覆盖不全 → 只 og:image（分享主图）命中；标题空或命中反爬 → 触发 incomplete + 手动面板。
- 方案：两层补全，复用已配的 Agnes Key，无需新增配置。
  1. 增强 HTML 提取：`extractEmbedded` 扩充键名（titleText/subTitle/itemName、salePrice/priceInfo/realPrice/lowPrice/highPrice、shopName/sellerName、originalImage）；`extractMeta` 合并 shop 并把 `//img...` 主图归一化为 https 绝对 URL。
  2. 视觉识别兜底：新增 `server/vision.js`，调用 **agnes-2.0-flash**（`/v1/chat/completions` + image_url，OpenAI 兼容，能读图做结构化提取）识别商品图 → `{title, price, garmentType, color, region}`。远程图先服务端下载转 dataURL 规避淘宝防盗链；超时+容错，失败返回 null 回退手动。
  3. `parse.js` 新增 `tryVision()`：在「有主图但文本缺失」的购物平台链接（被反爬/标题无服装词/缺价格/缺标题）时调用，自动填充标题/价格/garment；HTML 已确认的字段优先，缺失才用视觉补；结果标 `aiRecognized:true`。
- 前端：`types/index.ts` 加 `aiRecognized?`；`ProductCard.tsx` 显示「AI 识别」绿色徽标，并把"需手动确认"提示改为"AI 已通过商品图识别到部分信息…"。
- 配置：`server/.env.example` 加 VISION_MODEL=agnes-2.0-flash、VISION_TIMEOUT 及复用说明；`index.js` health 增 visionConfigured/visionModel 与启动日志。
- 验证：`node --check` 全过；vision 模块单测（mock fetch）确认 JSON 解析+颜色映射+类型回退正确；后端启动 health 报 visionConfigured:true（复用 IMAGE_API_KEY）；`npm run build` 通过；已 redeploy 同一 CloudStudio 链接。
- 注意：视觉识别与 AI 试衣一样需本地后端（key 在服务端）：`cd server && npm start` + `cd app && npm run dev` 才能端到端自动补全；纯静态部署站无后端时仍走 mock/手动。

## 第十一轮：修复视觉识别静默失败 + 增强天猫提取（2026-07-19 晚）
- 用户反馈：上一轮加了 vision 兜底后，测试仍然"只有图、其他没自动填充"
- **根因①（关键 bug）**：`agnes-2.0-flash` 是**推理模型（reasoning model）**，输出分 `reasoning_content`（思考过程，占大量 token）和 `content`（正式回答）。原 `max_tokens: 600` 不够 → 思考就用光 → `content=""` → `parseVisionJson` 拿空字符串返回 null → 静默失败回退手动选择
  - 修复：`vision.js` max_tokens 从 600 提到 **2048**；`parseVisionJson(text, reasoningContent)` 增加**从 reasoning_content 提取 JSON 的兜底**；recognizeProductImage 增加 `[vision]` 前缀日志记录 API 状态码、content/reasoning 长度、HTTP 错误详情
- **根因②（ESM dotenv 时序）**：所有 `import` 在 `dotenv.config()` **之前执行**。`vision.js` / `parse.js` 读 `process.env` 时 dotenv 还没跑 → `VISION_API_KEY=""` → `visionConfigured=false`
  - 修复：`index.js` / `vision.js` / `parse.js` 全部改为 `import dotenv from "dotenv"; dotenv.config({ path: join(dirname(fileURLToPath(import.meta.url)), ".env") })` —— 从自身文件所在目录加载 .env，不依赖 CWD
- **根因③（天猫反爬）**：部分天猫链接即使带 Cookie 也只返回 JS 中间跳转页（504B，含 `localStorage.x5referer` 脚本），无 og:image/标题/价格 → imageUrl 为空 → vision 触发条件不满足
  - 修复：新增 `isInterstitialPage()` 检测中间跳转页；检测到后自动用 **Mobile UA 重试**（iPhone UA）；仍无效则调用**轻量详情接口** (`detailskip.taobao.com`) 兜底；新增 `fetchLightDetail()` + `parseLightDetail()` 辅助函数
- 验证：
  - 直接调 `/v1/chat/completions`（agnes-2.0-flash）→ HTTP 200，确认模型返回 `reasoning_content`（含思考过程）+ 空 content（max_tokens 不够时）
  - 后端 health 报 `visionConfigured:true`（dotenv 修复生效）
  - Tmall 802805668523 实测：interstitial 页面被正确检测并触发 mobile UA 重试 + light detail API（该商品反爬太强，三个渠道均未拿到数据——属极端情况）
  - `node --check` 三文件通过；`npm run build` 通过；已 redeploy CloudStudio

## 第十二轮：修复试衣效果"两张图拼接" → 真试衣提示词优化（2026-07-19 晚）
- 用户现象：右侧"试衣效果"把用户的脸和商品模特图**拼在一起**（两张图都出现在输出中），不是真正试衣
- 根因（server/tryon.js）：有 productImage 时走**模式 A（换脸式）**
  - 图序 `images = [productImage, selfie]`（商品图第一张=底图，自拍第二张=提供脸）
  - 提示词说"保留第一张图姿势/服装，仅替换人脸" → 模型理解为"把两张图内容都放进去"
  - **用户实际要的**：以自拍为底图（完整保留脸/身体/姿势/背景），把商品衣服穿到身上
- 修复（仅改 server/tryon.js，不动前端/UI）：
  1. **图序反转**：`images = [selfie, productImage]` —— 自拍为第一张（底图），商品图为第二张（仅服装参考）
  2. **提示词完全重写**（模式 A）：明确标注「【第一张图片：用户本人】需完整保留脸部/发型/体型/姿势/背景」+「【第二张图片：商品服装参考图】仅提取款式颜色图案，忽略模特和背景」+ 5 条严格要求（同一个人、适配身材、自然融合、照片级真实、不出现商品图原始内容）
  3. **身体数据融入模式 A**：fitText 提取到两种模式共用，真试衣也根据身高/体重/三围调整版型
  4. JSDoc 更新为「真试衣」（非换脸式）
- 验证：`node --check tryon.js` 通过；`npm run build` 通过；后端重启 health 正常；已 redeploy CloudStudio

## 第十三轮：身体数据 localStorage 持久化（2026-07-19 晚）
- 用户需求：填写的身体数据（身高/体重/三围/肩宽/性别）刷新页面时丢失，要求本次存储避免丢失
- 实现（仅改 `app/src/App.tsx`，不动组件/后端）：
  - 新增 `MEASURE_KEY="tryon:measurements"` + `loadMeasurements()`（从 localStorage 读取并做类型校验：gender 只接受 female/male/空，数值缺失/NaN 归空）+ `saveMeasurements(m)`（try/catch 静默容错）
  - `measurements` 状态用 `useState(loadMeasurements)` 懒初始化（首屏即从本地恢复）
  - 新增 `useEffect(() => saveMeasurements(measurements), [measurements])`：任意改动即写入 localStorage
- 验证：`npm run build` 通过；已 redeploy CloudStudio

## 修复：首次生成拿到 mock 商品衣服、再试才真实（2026-07-19 晚）
- 现象：上传图片后第一次点「生成试衣」，穿的是 mock/通用衣服，再点一次才用真实商品图。
- 根因：`product.imageUrl` 是外链（img.alicdn.com 等），仅在生成时后端 `tryon.js#resolveToDataUrl` 才临时拉取；首次拉取常因冷连接/防盗链/超时失败→返回 null→走模式 B（文字描述通用衣服=mock）；第二次连接已热成功→模式 A（真实图）。前端 `generateTryOn` 的 catch 又静默回退本地 SVG，放大问题。
- 修复（三处）：
  1. `server/tryon.js`：`resolveToDataUrl` 改为带超时(AbortController 10s)+重试(2次退避)+合理 Referer(图片站点根域，防盗链)，并 `export`。
  2. `server/index.js`：`/api/parse` 返回前预取 `product.imageUrl`→dataURL 替换（解析阶段用户在填自拍/身体数据，有时间；试衣时直接命中 data: 分支，消除运行时拉取竞态）。
  3. `app/src/lib/tryon.ts`：`generateTryOn` 首次失败自动重试真实接口 1 次（NO_TOKEN/NO_MODEL 不重试），两次都失败才回退本地 Canvas。
- 验证：前端 `npm run build` 通过；后端 `node --check` 通过。

## 修复：本地只能用 localhost、127.0.0.1 访问不了（2026-07-19）
- 根因：`app/vite.config.ts` 未设 `server.host`，Vite 默认只绑定 localhost 主机名（macOS 常解析到 ::1），故 127.0.0.1(IPv4) 连不上。
- 修复：`server.host: true`（监听 0.0.0.0+::，localhost/127.0.0.1 均可访问）；同时把代理 target 从 `http://localhost:8787` 改为 `http://127.0.0.1:8787`，避免 Node 把 localhost 解析到 ::1 导致代理连不到后端。

## 新增功能：上传全身照 → AI 识别自动填充身体数据（2026-07-19）
- 需求：用户上传全身照片后，可点「AI 识别」自动估算并填充身体数据（身高/体重/三围/肩宽/性别）。
- 实现：
  - `server/vision.js`：复用 `toImageContent` + 模型配置，新增 `estimateBodyFromImage(imageInput)`，prompt 让视觉模型估算 7 项身体数据；新增 `parseBodyJson`（数字>0取整，性别只收 female/male）。
  - `server/index.js`：新增 `POST /api/estimate-body`（body:{image}）；未配置视觉模型返回 503 NO_VISION；识别失败 502。
  - `app/src/lib/api.ts`：新增 `estimateBodyViaApi(image): BodyEstimate`（数值为 number|null）。
  - `app/src/App.tsx`：身体数据卡片头部加「AI 识别」按钮（selfieUrl 存在才可用），`handleEstimateBody` 调接口后回填 measurement（已有值不覆盖，便于微调），成功 toast、NO_VISION/失败有提示。
- 注意：单张照片估算有误差，已在 UI 标注「仅供参考，可微调」；识别时会把照片发给视觉模型（已配置前提下）。
- 验证：后端 node --check 通过；前端 npm run build 通过。

## 调整主流程顺序：自拍+身体数据 前置，链接后置（2026-07-19）
- 需求：流程改为「① 上传自拍 → ② 身体数据 → ③ 粘贴链接 → ④ 生成试衣」。
- 改动：
  - `app/src/components/StepIndicator.tsx`：STEPS 改为 ["上传自拍","身体数据","粘贴链接","生成试衣"]。
  - `app/src/App.tsx`：`step` 计算改为依赖 canGenerate/generating（且置于 canGenerate 之后避免 TDZ）；自拍+身体数据卡片改为**始终可见**（去掉 `product?.isClothing && selectedGarment` 门控）；从 hero 移除 LinkInput，在 main 流程中以「粘贴商品链接」卡片置于自拍/身体数据之后；已识别商品卡片保留；FEATURES 顺序同步调整；hero 副标题改写。
- 验证：前端 npm run build 通过。

## 修复：生成图片被压扁/非原尺寸（2026-07-19 下午）
- 现象：原图是竖向全身照，生成后人物看起来"压扁了"，比例失真。
- 根因：`tryon.js#callImageApi` 中 `size` 固定为 `"1024x1024"`（正方形）。竖版自拍被强制塞入方形画布 → 模型输出为 1:1 正方形，人物比例被压缩。
- 修复：
  1. 新增 `getImageDimensions(dataUrl)`：从 PNG/JPEG 的二进制头解析原始宽高（PNG 读 IHDR chunk offset 16-23，JPEG 找 SOF0/C2 marker）。
  2. 新增 `pickBestSize(selfieDataUrl)`：根据宽高比自动选择 Agnes 支持的尺寸——ratio<0.85 用 `768x1024`（竖版），>1.15 用 `1024x768`（横版），否则 `1024x1024`（方形）；仍可被环境变量 `IMAGE_SIZE` 手动覆盖。
  3. `callImageApi` 中 `size = pickBestSize(selfie)` 替代原来的硬编码。
  4. 两种模式 prompt 均加强约束：「画面比例必须与原图完全保持一致，不得改变人物身形比例或画面宽高比」。
- 验证：后端 node --check 通过；前端 npm run build 通过。

## 修复：生成图片仍为正方形——双重根因（2026-07-19 下午）
- 现象：上次修了 pickBestSize 逻辑后，测试结果仍是 1024×1024 正方形（原图 1280×1707 竖版被压扁）。
- 根因（两层叠加）：
  1. **`server/.env` 第 36 行 `IMAGE_SIZE=1024x1024`**：pickBestSize() 第一行就检查环境变量，有值直接返回，跳过所有自动检测 → 智能选尺寸代码完全无效。
  2. **JPEG SOF 宽高解析读反了**：SOF 结构中 offset+5 是高度、offset+7 是宽度；旧代码写成 width=offset+5, height=offset+3 → 对原图测出 1707×4360（错误），碰巧 ratio<0.85 也选了竖版但数值完全不对。
- 修复：
  1. `.env`：注释掉 `IMAGE_SIZE=1024x1024`，留空让自动检测生效（保留注释说明可手动覆盖）。
  2. `tryon.js#getImageDimensions`：JPEG 分支修正为 `width=buf.readUInt16BE(offset+7)`, `height=buf.readUInt16BE(offset+5)`；清理多余 `}`。
  3. `callImageApi` 加 console.log 日志 `[tryon] 原图尺寸检测: 输出size=xxx` 方便排查。
- 验证：用真实原图测试 → 正确输出 {width:1280, height:1707} ratio=0.75 → 选择 **768x1024(竖版)** ✅；后端语法 + 前端构建均通过。

## 修复：试衣效果人物被重新生成（2026-07-19 下午）
- 现象：原图穿绿色套装，生成后变成绿色连衣裙；姿势改变（手从下垂变抬起）；体型被拉长——模型在"重新生成整个人物"而非局部换衣。
- 根因：agnes-image-2.0-flash 图生图模式收到两张图时会融合两者特征，旧提示词约束力不够（只有5条正面要求，无负面约束）。
- 修复（三维度）：
  1. **提示词重构**：模式A/B全部重写。
     - 引入「六维锁定」机制（面部/发型/体型/姿势/手势/背景），每维单独强调；
     - 新增「🚫负面约束」板块，用 ❌ 明确列出13条禁止行为（禁止换脸、改姿势、改手势、改背景、用模特替代用户、服装类型偏离等）；
     - 任务定义从「请生成」改为「执行局部编辑」；
     - 输出标准改为「像这个人真的去试穿了拍的照片」。
  2. **调用参数优化**：
     - `callImageApi` 新增 `negative_prompt` 参数（13条中文负面描述），配合正向 prompt 形成双向约束；
     - 增加调用日志（模型名/尺寸/图数量/是否有商品图）方便排查。
  3. 模式B同步加强：同样的六维锁定+负面约束结构。
- 验证：后端 node --check 通过；前端 npm run build 通过。

## 按 OpenAI 提示词方法论重构试衣 prompt（2026-07-19 傍晚）
- 需求：用户希望仿照 OpenAI image generation 的 prompt 设计方法论重写试衣提示词（之前是"六维锁定+负面约束"禁令式）。
- OpenAI 方法论核心：① 描述式而非指令式（Describe, don't command）② 分层结构（Subject/Environment/Lighting/Style/Composition/Mood）③ 官方不推荐 negative_prompt，建议把"保持不变"也用正向语言描述。
- 改动（server/tryon.js#buildPrompt）：
  - 模式A/B 全部重写为「角色 → 画面主体(图1人物, 分项正向描述) → 待试穿服装(图2) → 拍摄说明 → 画面规格 → 输出」的分层描述式结构。
  - 把"禁止改变X"改成"图1中X呈现特定特征，请原样保留"的正向表述。
  - callImageApi 的 negative_prompt 保留（兼容 agnes），但精简为8条兜底措辞，并在注释中说明 OpenAI 不推荐负面提示、正向描述才是主约束。
- 验证：后端 node --check 通过；前端 npm run build 通过。

## 功能：前端 build 产物直接输出到 server 一体托管（2026-07-19 傍晚）
- 需求：app 项目 build 时输出到 server 项目，server 启动即可直接托管前端（单进程部署）。
- 改动：
  1. `app/vite.config.ts`：`build.outDir = "../server/public"`、`emptyOutDir: true`；`base` 由 `"./"` 改为 `"/"`（绝对路径——SPA fallback 下深层路由刷新时相对路径会解析出错误资源路径 → 404，根路径托管必须用绝对路径）。
  2. `server/index.js`：import `existsSync` from fs、定义 `__dirname`/`PUBLIC_DIR`；在**所有 /api 路由之后**加 `app.use(express.static(PUBLIC_DIR))` + `app.get("*")` SPA fallback（返回 index.html）；public 不存在时打印提示。
  3. `server/.gitignore`：新增 `public`（构建产物，由 app build 生成不入 git）。
- 验证：前端 build 产物落入 `server/public/index.html`+`assets/`；用 PORT=8799 启动实测：`GET /`→index.html、`GET /assets/*.js`→200、`GET /api/health`→JSON、`GET /product/123`(深层路由)→200(SPA fallback)。前端 `api.ts` 默认 `API_BASE="/api"` 相对路径，同源一体部署完美互通。
- 使用方式：`cd app && npm run build` → `cd server && node index.js` → 访问 http://localhost:8787。dev 模式仍用 vite proxy（不影响 build）。

## 修正：Agnes 并非只支持 3 种尺寸——按原图实际尺寸生成（2026-07-19 傍晚）
- 用户质疑：`server/tryon.js#pickBestSize` 写死 3 种尺寸（768x1024/1024x1024/1024x768）是否必要。
- 实测 Agnes API（文生图逐个试 size）：
  - `512x512`→200 实际输出 `1024x1024`（被归一化）；`1024x1536`→200 实际输出 `832x1248`（**等比 0.6667 保留**）；`1280x1707`(原图精确)→**503 Service busy（瞬时繁忙，非尺寸拒绝）**；`768x1024`/`1080x1920`→503 同样瞬时繁忙。
  - 结论：Agnes 接受**任意 WxH**，且**保持请求宽高比**（仅归一像素总量），故"按原图实际尺寸"完全可行，且根治"压扁"。
- 改动（`server/tryon.js#pickBestSize`）：去掉 3 档硬凑，改为返回 `${自拍真实宽}x${真实高}`；超大图（长边>2048=`MAX_EDGE`）按比例缩放到 2048 避免超时；仍保留 `IMAGE_SIZE` 环境变量手动覆盖与解析失败回退 1024x1024。同步更新函数注释。
- 验证：后端 node --check 通过；前端 npm run build 通过。

## 修复：生成图变成另一个模特——提示词三重重构（2026-07-19 傍晚）
- 现象：原图是短发穿绿色套装的中年女性，生成后变成长发年轻女性（商品图模特）——身份被完全替换。
- 根因（三层叠加）：
  1. **图序策略缺陷**：传两张图 [自拍, 商品图]，商品图中含模特→模型融合了模特的外貌
  2. **角色框架错误**：用"摄影师生成新照片"的语义→模型理解为 GENERATE 而非 EDIT，自由发挥
  3. **提示词过长**：~500字分层结构，"只换衣服"的核心指令被淹没
- 修复（server/tryon.js）：
  1. **提示词全面重写**：角色从「专业时尚摄影师」→「图像编辑器 IMAGE EDITOR」；任务定义改为「只执行一个操作：把衣服换成XX」；精简到~200字；新增「绝对不可改变的内容」板块+明确说明「忽略第二张图的模特外貌仅提取服装信息」。
  2. **新增人物外貌自动提取**：`extractFaceDescription(selfie)` 函数，在调用图像生成前先用视觉模型(agnes-2.0-flash)分析自拍中人物的发型/脸型/体型/穿着/姿势等可见特征（如"黑色短发、椭圆脸、标准体型、穿绿色家居服、双手自然下垂"），嵌入 prompt 作为具体锚点。实测输出准确且有用。失败不阻塞主流程。
  3. `runTryOn` 改为先提取外貌描述再构建 prompt 并传 `faceDescription` 参数。
- 验证：后端 node --check 通过；前端 npm run build 通过；外貌提取功能实测正常输出准确特征描述。
